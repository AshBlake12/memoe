# Analysis + simulation + dashboard. CPU-only; reproduces everything in results/.
# Runtime benchmarks need extras: see pyproject.toml [project.optional-dependencies].
numpy>=1.24
pandas>=2.0
pyyaml>=6.0
matplotlib>=3.7
tabulate>=0.9
plotly>=5.18
pytest>=7.0
