# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/

# Build / scratch
*.tgz
*.log
gem5-src/
.pytest_cache/

# Large inputs that are re-derivable or fetched, not authored here.
traces/*.parquet
traces/data/

# results/dashboard.html is committed on purpose so it opens without a Python
# environment.

# SystemC build artifact (rebuild with: cd systemc && make)
systemc/memoe_tlm
